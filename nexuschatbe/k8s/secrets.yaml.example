apiVersion: v1
kind: Secret
metadata:
  name: nexuschat-secrets
  namespace: nexuschat
type: Opaque
stringData:
  # TEMPLATE - Copy this to secrets.yaml and fill with real values
  # The real secrets.yaml is gitignored for security
  
  app-key: "base64:YOUR_APP_KEY_HERE"
  
  db-root-password: "your-secure-root-password"
  db-password: "your-secure-db-password"
  
  pusher-app-id: "your-pusher-app-id"
  pusher-app-key: "your-pusher-app-key"
  pusher-app-secret: "your-pusher-app-secret"
